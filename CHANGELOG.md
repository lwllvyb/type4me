# Changelog

## v2.6.1 — 悬浮窗文字布局与边缘优化 (2026-09-05)

- **状态文字居中**：按实际可用文字区域居中显示“倾听中”，显示或隐藏取消按钮时均使用对应的边界。
- **文字间距一致**：根据录音球与取消按钮的可见边缘计算间距，缩小录音球一侧的空隙，并为隐藏取消按钮时的文字右侧保留留白。
- **双侧渐变**：长转写文本两端增加渐隐效果，避免文字紧贴悬浮窗边缘。
- **轻微投影**：为悬浮窗增加柔和阴影，让浅色和纯白背景下的边界更清晰。

## v2.6.0 — 首启引导、Meta Muse 与录音体验 (2026-09-04)

v2.6.0 简化首次设置，扩展实时语音识别，并改善录音反馈、媒体播放和大型词汇列表体验。

### 新功能

- **两步首启与权限引导 (#281)**：首次使用只需完成欢迎与权限设置；提供辅助功能拖拽授权引导，并在需要时提示重新启动以恢复快捷键。

<p align="center">
  <img src="https://github.com/user-attachments/assets/793474bf-8c4c-4430-88c4-27a1a22c436b" width="640" alt="两步首启流程的欢迎页面" />
</p>

<p align="center">
  <img src="https://github.com/user-attachments/assets/b2325f38-09c0-4f24-8ed8-353cb21f7d23" width="640" alt="权限设置页面（PR 截图）" />
</p>

> 配图来自 PR；当前版本的音频处理方式取决于所选语音识别服务，使用云端服务时会上传音频进行识别。

<p align="center">
  <img src="https://github.com/user-attachments/assets/33baf93a-0139-4f93-874b-93afd99c7d0b" width="720" alt="将 Type4Me 拖入系统设置的辅助功能列表以完成授权" />
</p>

- **首页模型配置提醒 (#281)**：默认语音识别或文本处理服务尚未配置时，首页显示可直接跳转到对应模型设置的提醒卡片，保存配置后自动消失。

<p align="center">
  <img src="https://github.com/user-attachments/assets/c1791739-675c-4f97-921d-7f0169655e24" width="720" alt="首页的语音识别与文本处理模型配置提醒" />
</p>

- **Meta Muse 实时语音识别 (#286)**：接入 Muse Voice Transcribe，支持流式转写、热词偏置与语言提示，并处理连接准备和正常断开时的状态反馈。
- **随语音变化的录音球 (#285)**：液态玻璃录音球随说话节奏响应，安静时保持平缓；优化小尺寸边缘清晰度和不同动效预设的一致性，继续尊重静态样式与“减少动态效果”设置。

### 改进与修复

- **语音输入时保留媒体播放 (#283)**：修复未设置音量选项时意外静音的问题；选择内置麦克风时，不再因保活逻辑误启用系统默认的蓝牙麦克风。优先设备不可用时避免静默切到蓝牙输入，明确选择的设备在启动时消失则安全停止。
- **终端输入反馈 (#280)**：修复终端或自定义渲染应用中已经粘贴成功，却仍提示“未找到可输入的位置”的误判。
- **片段替换列表更流畅 (#284)**：按需加载可见行，缩小悬停与编辑的刷新范围，并减少重复搜索计算，改善大量词条下的页面打开、滚动和编辑体验。
- **权限变更与配置稳定性 (#281)**：改进运行中撤销权限时的快捷键监听恢复与释放，修复凭证变更通知可能引发的重入死锁；权限和配置提示支持中英文即时切换。
- **开发部署兼容性 (#286)**：系统限制启动会话时，部署脚本可通过系统打开命令回退启动应用。

## v2.5.0 — 模型设置、精准写入与原生液态玻璃 (2026-09-01)

v2.5.0 重做模型与模式设置体验，让手动录音可以安全写入结束录音时聚焦的输入框，并将录音指示条升级为系统原生 Apple Liquid Glass。

### 主要亮点 (Highlights)
- **全新模型与模式设置 (#277)**：语音识别与文本处理服务改用统一的主从布局，按推荐、本地与云端分组展示服务商，并加入官方品牌图标、配置状态、密钥显隐以及更清晰的“设为默认”、测试、保存和还原操作；模式设置同步采用同一套交互语言

<p align="center">
  <img src="https://github.com/user-attachments/assets/8e25aba8-9c35-46fa-893c-9ea8fd8cad3f" width="900" alt="全新的语音识别服务设置界面" />
</p>

<p align="center">
  <img src="https://github.com/user-attachments/assets/fa98e1da-9fc8-4f93-8e75-6030797a79b1" width="900" alt="全新的文本处理服务设置界面" />
</p>

- **录音结束时的输入框 (#269)**：手动录音新增“写入目标”选项，可保持写入录音开始时的应用，也可在停止录音时将文字写入当前明确聚焦的输入框；粘贴前会重新验证应用、窗口和控件，目标变化或无法安全确认时保留到剪贴板
- **原生 Apple Liquid Glass 录音条 (#279)**：macOS 26 使用原生 Liquid Glass 材质，支持深色与明亮两档主题和设置页实时预览；macOS 14/15 自动回落为原生磨砂，开启“减少透明度”时使用实色背景，无需新增屏幕录制权限

### 改进与修复 (Improvements & Fixes)
- **模型配置更安全 (#277)**：浏览服务商与切换默认引擎彼此独立，未保存修改会进入统一的离开确认流程；支持清空并保存凭证以彻底移除错误配置，测试连接不会再隐式保存草稿
- **中英文设置侧栏不换行 (#278)**：固定导航标签为单行显示，并让版本号独立截断，避免英文 “Settings” 或版本信息挤压侧栏布局

<p align="center">
  <img src="https://github.com/user-attachments/assets/9914d81c-8a74-4208-aff2-218de54117c6" width="243" alt="英文设置侧栏单行标签" />
  <img src="https://github.com/user-attachments/assets/c7775843-09ce-4aea-9a12-d5b4c0bdaf19" width="245" alt="中文设置侧栏单行标签" />
</p>

- **录音目标归属与安全回退 (#269)**：结束时目标模式会把 Intelli Sense 环境与历史记录归属到实际输入位置；URL 自动化等专用路径继续沿用既有语义，处理期间目标变化时不会误粘贴
- **录音条主题与布局稳定性 (#279)**：深色主题加入稳定的对比度下限，主题切换只重建预览中的玻璃表面；Tooltip 等浮层展开或收起时保持胶囊居中，减少像素偏移与视觉闪动

## v2.4.0 — 实时 ASR、毛玻璃录音与模式管理 (2026-08-30)

v2.4.0 扩展两项低延迟云端语音识别服务，并为录音浮窗、首页模式管理和历史反馈带来一轮更完整的体验升级。

### 主要亮点 (Highlights)
- **Gemini 3.5 Transcribe 实时语音识别 (#265)**：新增 Gemini Live API 流式识别，支持 Smart / Verbatim、语言提示和热词；从录音到最终文本均保持实时处理路径
- **阶跃星辰实时语音识别 (#270)**：除既有批量服务外，新增开放平台 WebSocket 实时 ASR；录音在握手期间会安全缓存音频，连接就绪后自动补发，快速点按取消也能正确收尾
- **浅色主题与原生毛玻璃录音条 (#274)**：录音浮窗新增浅色主题，并以 macOS behind-window 材质实现真实的桌面透光与玻璃边缘；深色主题仍为默认，现有用户无需迁移

<p align="center">
  <img src="https://github.com/user-attachments/assets/63e0bd41-3d79-482f-a0e0-e48c6037dbf0" width="900" alt="浅色原生毛玻璃录音条与外观设置预览" />
</p>

- **首页就地管理模式 (#271)**：在首页“我的模式”卡片中直接新增、编辑、删除和排序快捷键；整行点击可进入对应模式，拖动、悬停与删除操作不会造成布局跳动

<p align="center">
  <img src="https://github.com/user-attachments/assets/ed1b714f-fbaf-41ac-92dc-546e0f0e5428" width="900" alt="首页使用概览与就地管理模式快捷键" />
</p>

- **紧凑型实时文本 (#262)**：Compact 指示条开启“实时展示文本”后会在固定宽度内扩展为上下两行，实时跟随最新转写；处理、完成与错误状态仍保持原有紧凑高度

<p align="center">
  <img src="https://github.com/user-attachments/assets/f231b4a7-80da-473b-9326-1d270af9e8e7" width="640" alt="紧凑型录音指示条中的实时识别文本" />
</p>

- **历史识别信息与质量反馈 (#259, #268)**：可在录音条显示模式、服务商和模型名称；历史用量按模型展示，并可将识别不佳的记录标记为负反馈，在统计中查看数量与比例

<p align="center">
  <img src="https://github.com/user-attachments/assets/c962124b-521d-4985-aac4-cb94d2c63d94" width="760" alt="按模型查看历史识别用量" />
</p>

<p align="center">
  <img src="https://github.com/user-attachments/assets/892fcdc0-59bf-4923-8f04-515804c426dc" width="240" alt="历史记录中的识别质量负反馈" />
</p>

### 改进与修复 (Improvements & Fixes)
- **语音润色不再回答问题 (#263)**：将润色规则与原始口述文本分离发送，明确把问句和命令当作待润色内容，避免模型误将它们当成请求并直接回答或执行
- **Deepgram 与历史信息更完整 (#259)**：支持自定义 Deepgram 模型；直连/兼容端点会使用正确鉴权方式，历史记录和用量详情会稳定显示实际 ASR 模型
- **设置交互更细腻 (#274)**：统一设置页 Tooltip、液态玻璃标签切换与内联分段控件的动画和无障碍降级；剪贴板留存设置拆分为更易理解的常规输入与取消行为控制

## v2.3.0 — 液态玻璃录音体验与首页数据概览 (2026-08-26)

v2.3.0 为录音浮窗带来全新的 Liquid Glass 视觉与更细腻的交互，并重做首页，让使用数据和快捷入口更清晰地呈现。

### 主要亮点 (Highlights)
- **Liquid Glass 录音指示条 (#260)**：常规录音条升级为液态玻璃视觉；录音球和实时文本共享材质与光效，提供多种动态预设，以及不持续占用 GPU 的静态 Siri 节能方案

<p align="center">
  <img src="https://github.com/user-attachments/assets/3c5244e1-f688-4a61-86b7-328ddeec1275" width="480" alt="Liquid Glass 录音指示条与取消录制交互" />
</p>

- **可定制的录音外观**：可独立选择是否显示操作提示与取消按钮；外观页的实时预览会立即反映设置，隐藏取消按钮后仍可使用 `Esc` 取消录音

<p align="center">
  <img src="https://github.com/user-attachments/assets/9970a2f9-656f-4504-b23d-360c33fa226f" width="900" alt="外观设置中的提示与取消按钮开关" />
</p>

- **全新首页使用概览 (#258)**：首页将使用概览、活跃记录与快捷键集中到更紧凑的布局；新增随窗口宽度适配的活动热力图，悬停即可查看每日听写时长和字数

<p align="center">
  <img src="https://github.com/user-attachments/assets/34fbf973-df5a-4b8e-90de-e62cc08683e8" width="900" alt="首页使用概览、活动热力图与快捷键" />
</p>

### 改进与修复 (Improvements & Fixes)
- **录音条交互更自然**：取消按钮采用玻璃质感与流体按压、拖动反馈；长文本转写时胶囊布局保持稳定，避免文字与操作按钮互相挤压
- **预览与动效更可靠**：外观预览中的控件不会再将预览状态锁死；遵循“减少动态效果”偏好，并让静态样式不产生持续动画开销
- **活动数据更易读**：热力图月份标签与日期对齐，悬浮详情加入延迟阈值和淡入淡出，减少快速移动时的闪现

## v2.2.0 — 菜单栏控制、自动化录音与外观升级 (2026-08-25)

v2.2.0 将运行中的常用控制集中到菜单栏，并为自动化录音、更多语音识别服务和录音条外观带来完整升级。

### 主要亮点 (Highlights)
- **菜单栏运行控制中心**：可直接查看录音状态与有效输入设备，快速切换模式、麦克风、ASR 和翻译目标，并访问录音控制、历史、改口、权限、更新和工作区
- **URL Scheme 自动化录音 (#194)**：支持 `type4me://start`、`stop`、`toggle`，可供 Stream Deck、快捷指令、Raycast、Alfred、BetterTouchTool、Hammerspoon 和脚本免模拟键盘事件调用
- **小米 MiMo-V2.5-ASR (#211)**：新增 MiMo 批量语音识别服务；设置与菜单栏为所有非实时服务提供统一标记
- **外观设置与实时预览**：新增独立的外观页面，将录音动效、实时文本、悬停预览和文本格式选项集中管理，并用真实录音条和格式化结果即时预览效果

<p align="center">
  <img src="https://github.com/user-attachments/assets/ad6a3d2e-3a4c-4d83-90af-56f0abc3dd74" width="900" alt="外观设置中的录音条与文本输出预览" />
</p>

- **紧凑型录音指示条**：新增与录音动效独立的 Compact 样式，以更小的声纹胶囊呈现录音、处理和完成状态

<p align="center">
  <img src="https://github.com/user-attachments/assets/e3a3dbd8-e08c-41f3-bc13-c49cf478d1d5" width="900" alt="紧凑型录音指示条及完成和取消操作提示" />
</p>

### 改进与修复 (Improvements & Fixes)
- **跨 App 文本注入安全性**：注入前确认目标 App 已真正回到前台；目标已退出或无法确认时安全保留到剪贴板，避免口述内容粘贴到错误应用
- **剪贴板留存策略**：按正常提交、失败和取消等结果分别处理剪贴板；取消录音时不再显示多余的处理界面
- **自动化调用更可靠**：URL 后台调用不抢占前台焦点，冷启动不弹出设置向导；准备阶段可以安全停止，任何录音路径均可用 ESC 取消
- **批量识别韧性**：严格处理 MiMo 的异常终止结果，并在批量识别失败时丢弃未确认的 partial 文本
- **界面与本地化**：系统模式名称随语言设置即时切换，设置导航稳定，英文快捷键录制控件不再换行

## v2.1.1 — 语音识别扩展、输出控制与稳定性 (2026-08-23)

v2.1.1 在保持 v2.1.0 全新模式架构的基础上，扩展可选语音识别服务，进一步缩短连续处理的等待时间，并修复历史记录、录音启动与快捷键的可靠性问题。

### 新功能 (New)
- **Cartesia Ink-2 流式语音识别**：新增 Cartesia 流式 ASR 服务，可在设置中配置并使用
- **每模式标点处理**：所有会粘贴文本的处理模式可分别设置标点策略，支持跟随通用设置、保留全部、去掉句末、仅保留问号和感叹号、去掉全部标点
- **可配置 ASR 服务地址**：Deepgram WebSocket 地址与自定义 OpenAI ASR Base URL 均可按服务商配置，便于使用兼容网关或私有部署

### 改进 (Improvements)
- **更快的连续 LLM 处理**：复用 Codex App Server 会话，减少连续语音处理的启动开销
- **OpenRouter 推理控制**：可为 OpenRouter 服务商关闭推理输出
- **历史记录信息更完整**：历史记录卡片会显示实际使用的识别与处理模型
- **文本输出一致性**：盘古之白中英文空格与中文直角引号转换会继续应用于模式级标点处理后的最终输出

### 修复 (Fixes)
- **USB 输出设备刷新崩溃 (#246)**：按 CoreAudio 返回的实际字节数分配输出设备的 `AudioBufferList`，避免多音频缓冲设备越界写入
- **录音启动延迟**：上下文读取不再阻塞录音开始
- **历史记录资源泄漏**：正确关闭 HistoryStore 的 SQLite 句柄，避免长期使用后耗尽文件描述符
- **修饰键快捷键可靠性**：修复独立修饰键与组合键的误触，并正确区分左右侧修饰键

## v2.1.0 — 8 种默认模式、设置重构、Intelli Sense、统一翻译与语音改口 (2026-08-18)

v2.1.0 全面刷新了设置与录音交互体验，提供 8 种开箱即用的默认模式，新增上下文感知写作、持久化语音问答、多语言翻译与语音改口能力。

### 主要亮点 (Highlights)
- **8 种内置默认模式**：提供快速模式、智能感知、翻译模式、随便问、Mac 操作、语音润色、Prompt 优化与代办模式，覆盖纯打字、智能整理、跨语言、选中文本问答、系统操作到成品交付全场景
- **全新设置与录音交互**：重构首页、模式、历史、词汇和模型设置，提供更清晰的录音与处理状态反馈
- **多快捷键支持**：同一模式可绑定多个「按住说话 (hold)」或「按一下开始/按一下停止 (toggle)」快捷键；录音期间可直接按下目标模式快捷键切换处理方式
- **Intelli Sense**：结合当前 App、输入控件与有限上下文进行安全润色；可独立开启并学习稳定的纠错、表达与列表结构偏好（各项感知与学习默认保持关闭）
- **Ask Anything 会话历史**：围绕选中文本直接提问，支持在设置中搜索、恢复、重命名、删除历史会话，并可直接从历史会话继续语音追问
- **统一翻译模式**：自动识别输入源语言，支持 18 种目标语言翻译；在输出语言不符时自动校验并重试一次
- **语音改口 Revise**：对最近一次仍可定位的 Type4Me 输入，按下改口快捷键（默认 `Fn+R`）直接说出修改指令，精准替换、局部微调、安全防篡改并支持一键/语音撤销

### 改进 (Improvements)
- **最终文本格式化**：可选盘古之白中英文空格与中文直角引号转换，同时保留英文撇号
- **录音浮窗体验**：新增录音浮窗实时转写显隐控制选项
- **词汇自动化**：支持通过 URL 命令打开词汇管理并重新加载外部 Skill 或脚本同步的词表
- **资源与性能优化**：改进不活跃设置页与 Ask Anything 资源释放，复用 LLM 客户端连接，压缩运行时缓存

### 修复 (Fixes)
- **火山 ASR 转写**：修复火山引擎流式识别在部分场景下产生重复修订快照的问题
- **Fn 点按与按键响应**：恢复可靠的 Fn 快速点按录音完成及 HID-first 按键响应
- **登录项注册**：修复并拒绝无效的裸可执行文件登录项注册
- **剪贴板历史污染**：Type4Me 内部剪贴板写入不再污染第三方剪贴板历史记录

### 升级说明 (Upgrade Notes)
- 升级至统一翻译模式时，系统会自动迁移并保留原有的翻译语言偏好设置
- Intelli Sense 上下文感知与偏好学习默认保持关闭（opt-in）
- 语音改口默认快捷键为 `Fn+R`；若升级前已存在快捷键冲突，改口快捷键将保持未分配状态供用户手动配置

## v2.0.0 — Selection Ask、Codex CLI 与多引擎语音识别 (2026-08-07)

### 新功能
- **Selection Ask**：针对当前选中文本直接提问，支持流式 Markdown 回答、连续追问与上下文保持
- **Codex CLI 文本处理**：可使用本机已登录的 Codex CLI，无需单独配置 API Key；请求会消耗 Codex 账号额度
- **更多语音识别服务**：新增 Grok 流式语音识别和 StepFun 批量转写
- **Qwen3-only 本地模式**：Local 版可以只使用 Qwen3-ASR，按需关闭 SenseVoice
- **应用内 GitHub 反馈**：可在设置中直接整理日志并提交问题反馈

### 改进与修复
- **火山新版鉴权**：支持新版控制台 API Key，同时保留旧版 App ID + Access Token；升级不再主动删除旧凭证
- **历史用量详情**：优化识别时长的分组、统计与表格布局
- **Grok 稳定性**：修复重复片段、抢跑结束、多句丢失和 retake 去重等流式转写问题
- **ElevenLabs 英文转写**：修正英文标点和空格，避免被错误规范化为中文标点
- **蓝牙录音与诊断**：改进蓝牙录音资源释放，并补充诊断日志和错误上报

## v1.9.9 — Soniox v5、录音启动稳定性与模型列表更新 (2026-07-03)

### 改进与修复
- **Soniox 升级到 v5**：实时识别默认使用 `stt-rt-v5`，批量兜底使用 `stt-async-v5`
- **录音启动更稳**：缩短 Soniox endpoint delay，并丢弃延迟过久的非最终 partial，减少启动/收尾阶段转圈或旧文本回流
- **快捷键兼容性**：修复 F1-F20 等功能键被 macOS 附带 Fn 标记后，录制、冲突检测和触发不一致的问题；普通按键的 Fn 组合仍保留
- **模型列表更新**：刷新 OpenAI、Claude、Gemini、DeepSeek、Kimi、豆包、MiniMax、百炼、智谱等 LLM 下拉模型；同步更新 AssemblyAI、Deepgram、ElevenLabs、百炼、百度等 ASR 模型/参数选项
- **思考模式参数修正**：Kimi K2.7 不再发送禁用思考参数，智谱 GLM 新模型改用 `thinking: disabled`

## v1.9.8 — 提前润色请求节流与复用优化 (2026-06-19)

### 改进
- **提前润色更稳**：保留现有录音中 speculative LLM 机制，但对过短文本、小幅增量和并发中的重复请求做节流，减少无意义 LLM 请求
- **LLM 结果复用更准确**：最终 ASR 与提前润色输入只有空白、标点或尾部标点差异时可复用已有 LLM 结果；真正新增/改写内容仍会重新请求
- **测试覆盖**：新增 `TranscriptDiff` 与 `SpeculativeLLMThrottle` 单元测试，覆盖复用判断、请求节流、排队和重置行为

## v1.9.7 — 凭证保存、快捷键冲突与录音动效修复 (2026-06-19)

### Bug 修复
- **凭证保存兼容性 (#193/#191/#146)**：兼容旧版 flat keys、provider-grouped JSON、Keychain secure fields，并自动补齐 provider 默认字段，修复升级后 ASR/LLM 凭证可能无法保存或测试/保存按钮不可用的问题
- **快捷键前缀冲突 (#192)**：检测单修饰键与组合修饰键的前缀冲突，设置页显示警告并允许「仍要设置」；运行时只延迟较短冲突键，避免误触发导致浮窗不出现或录音丢失
- **Fn 组合键 (#158)**：快捷键录制、显示和运行时匹配都保留 Fn/function 修饰键，支持 Fn+Space 等组合
- **录音动效 (#185)**：录音浮窗正确跟随线条 / 粒子云 / 电平样式，并新增「关闭 / Off」选项；关闭后仍保留处理阶段状态反馈
- **测试连接布局 (#142)**：失败时按钮显示紧凑重试状态，长错误信息显示在按钮下方，不再撑坏布局
- **应用内更新 (#203)**：下载遇到系统 “too many open files” 时给出明确重启/手动安装建议，并清理旧下载会话，降低重试失败概率
- **火山 ASR 错误提示 (#147)**：忽略 HTTP 探测产生的误导性 `cannot upgrade to websocket` 报错，避免把探测错误展示成真实连接失败

## v1.9.6 — 麦克风优先级 + 语音识别设置改进 (2026-06-15)

### 新功能
- **麦克风输入优先级**：语音识别设置可指定多个输入设备，Type4Me 会按顺序选择当前可用设备；蓝牙麦克风断开时自动回退到下一个设备，最后跟随系统默认
- **中英文空格设置**：语音识别设置新增「中英文空格」开关，可按需保留或去除中文与英文/数字之间的空格

### 改进与修复
- 修复应用内安装失败后可能回滚到旧版本、或重新打开 `Type4Me-backup` 的问题
- 本地版大包更新增强：放宽下载超时、自动断点重试，并补强进度条显示兜底
- 保留中英文和数字之间的合理空格，同时继续清理 CJK 内部拼接产生的多余空格 (#186)
- 修复 LLM 默认模型下保存/测试凭据按钮不可用的问题
- LLM 请求复用客户端和 URLSession，减少重复初始化开销
- 麦克风优先级弹窗优化为更紧凑的单列表布局，并扩大整行选择和右侧排序/增删按钮热区

## v1.9.5 — 更新串包与长闲置重连修复 (2026-06-02)

### Bug 修复
- **本地版应用内更新串包 (#184)**：Local 版现在会下载 `local-apple-silicon` DMG，不再误装成云端版导致 Qwen3-ASR / SenseVoice 本地识别不可用
- **火山 ASR 长时间不用后重连失败 (#181)**：WebSocket 首次发送失败时会用全新 URLSession 重试一次，并忽略 HTTP 探测产生的误导性 `cannot upgrade to websocket` 报错
- **耳机/媒体键触发打开 Apple Music (#180)**：注册活跃媒体会话拦截 transport media keys，避免 macOS 自动唤起 Apple Music
- **Qwen3 热词泄漏**：降低 Qwen3-ASR 把热词列表或 `Vocabulary` 前缀当成正文输出的概率，并在客户端做保守清理/回退
- 设置页快捷键录制弹窗的触发样式按钮扩大点击区域，整行按钮都可点击

## v1.9.4 — Mac Action + LLM 设置增强 + Local 稳定性 (2026-06-01)

### 新功能
- **Mac Action 模式**：用语音执行 macOS 操作，支持窗口管理、提醒事项、滚动、网页搜索、音量/亮度调整等常见动作
- **LLM 设置增强**：新增自定义 OpenAI 兼容服务商，支持从 `/v1/models` 自动发现模型；思考模式改为跟随服务商能力显示，可在支持的服务商上关闭思考输出
- **ASR 用量详情**：历史记录的「累计时长」新增详情面板，可按模型/引擎查看近 1 天、7 天、30 天本机识别时长
- **耳机/媒体键快捷键**：支持用耳机或键盘媒体键触发录音

### 改进与修复
- **Local 版本稳定性提升**：修复 SenseVoice + Qwen3 长时间使用时的内存增长、Qwen3 服务重启竞态，以及 MLX 0.31+ 兼容问题
- **Local 版本开箱即用加固**：Qwen3-ASR 继续使用 Python 3.12 + MLX 源码 JIT 构建，避免旧系统上的 metallib 兼容问题
- **ElevenLabs 体验改进**：启用更干净的转写结果，并自动限制 keyterm 数量和长度，避免请求失败
- 移除未采用的视觉、词汇和侧边打包实验改动，保留稳定功能进入正式版本

## v1.9.3 — 历史性能修复 + 代办模式 (2026-04-26)

### 新功能
- **代办模式 / Handle It**：直接交付型 AI 助手，把语音口述当成需求指令而非待润色文本。覆盖邮件、即时消息、代码、翻译等多种场景，强制只输出最终成品（无引导语、无反问、信息不全用 `[占位符]` 标出）

### Bug 修复
- **历史记录卡顿、白屏、内存持续上涨 (#144)**：bjzhush 反馈在历史记录里翻动 1 分钟内内存达 1+ GB。三层叠加根因：
  - `LazyVStack` 嵌套普通 `VStack/ForEach`，某天的所有 record cards 一次性 instantiated（不是真 lazy）
  - `groupedRecords` / `filtered` 是 computed property，每次 body 求值都重新 filter + group + sort
  - `ProgressView` 用 `.id("load-more-\(records.count)")` + `onAppear`，加载完触发 view 重建再次触发 onAppear，可能死循环加载到底
- **浮窗 hover 状态泄漏**：NSTrackingArea 在 panel hidden 时挂起事件不触发 mouseExited，`isHovered` 跨录音 session 保留，导致下次开始录音误弹悬浮预览

### 工程改进
- 订阅功能（official variant）归档：`Type4Me/CloudSubscription/marker` 永久重命名，默认构建走 pure 路径，public GitHub Release 只发 pure + local 两个变体

## v1.9.2 — 授权引导重做 + 历史日期筛选 (2026-04-17)

### 新功能
- 全新授权引导：首次启动或权限丢失时弹出统一的授权窗口，参考 Codex Computer Use 的拖拽式交互 — 点「授权」后浮窗跟随系统设置窗口展示，从浮窗里把 Type4Me 图标直接拖到列表即可完成辅助功能授权。拖入成功后自动返回主引导窗，两项权限都完成后一键「启动 Type4Me」
- 历史记录日期筛选：新增日期筛选菜单，支持今日/昨日/本周/本月预设，也支持自定义范围

### Bug 修复
- 修复从 1.8.x 升级到 1.9.x 后辅助功能授权「显示已开启但实际失效」的问题 (#135) — 根因是 1.9.0 切到 Developer ID 签名后，macOS TCC 按签名身份绑定授权，旧版自签条目对新签名无效。新的引导窗会主动识别并指导重新授权
- 历史记录搜索栏右侧按钮字号/基线对齐问题：统一到 12pt + 固定 30pt 高度

### 改进
- 麦克风在 `.denied` 状态下跳转系统设置并保持状态同步（guide 窗 1s 轮询，不再需要手动重启 app 才能看到状态更新）
- 浮窗以 60fps 跟随系统设置窗口位置，拖动窗口时浮窗紧贴不漂移

## v1.9.1 — 悬浮预览 + 应用级片段 + ASR 连接池 (2026-04-13)

### 新功能
- 悬浮文本预览：鼠标悬停录音条时弹出完整转录文本（设置可关）
- 应用级片段替换：针对不同 App 设置不同的替换规则，Chrome 风格 Tab 切换
- Prompt 优化器 v2：任务分类、智能框架扩展、结构化输出规则
- 正式写作 Prompt 升级：单要点内容不再强制编号，新增示例，旧版自动迁移
- Qwen3-ASR 服务崩溃自动重启（最多 3 次）
- 模型内嵌部署：SenseVoice/VAD 模型随 app 打包，首次启动自动部署

### 性能优化
- ASR 连接预热：启动时预建 TCP+TLS 连接，首次录音延迟降低 150-300ms
- 共享 URLSession 连接池：所有 ASR 客户端复用连接，减少 TCP 握手开销
- 火山 ASR 丢弃 partial 检测（LCP 算法），本地保留 partial 防闪烁
- SenseVoice 代际过滤 + VAD 残余样本处理，提升识别稳定性
- Cloud 配额非阻塞检查，录音启动不再等待网络

### 改进
- 设置页重组：录音行为拆分为"录音"和"语音识别"两张卡片
- 模式设置支持滚动，保存按钮移到顶部，显示模板变量提示
- 快速纠错后自动导航到词汇表 Tab
- 反馈音统一满音量，移除 Speaker Keep-Alive 功能
- Fn 键可用作快捷键修饰键
- 选中文本读取超时增加到 500ms，减少误判

### Bug 修复
- 剪贴板恢复时序修复（50ms→300ms），改善 VS Code/Slack/飞书等 Electron 应用兼容性
- Claude LLM 错误处理：检测 SSE error 和 stop_reason: "error"，60s 超时保护
- SQL 注入防护：HistoryStore 改用参数化查询
- 系统音量崩溃恢复：启动/退出时自动还原因崩溃未恢复的音量
- 录音状态管理：区分 preparing/recording 阶段，session idle 等待防止状态污染
- 浮窗动画竞态修复：generation counter 防止 hide/show 冲突

## v1.9.0 — Developer ID 签名 + 蓝牙支持 + 注入改进 (2026-04-11)

### 重要变更: Apple Developer ID 签名
本版本起使用 Apple Developer ID 签名并通过 Apple 公证 (Notarization)。安装不再提示"已损坏"或需要手动信任。

**升级用户请注意**: 由于签名身份变更，首次启动需要重新授予辅助功能和麦克风权限，以及确认钥匙串访问（输入 Mac 登录密码放行即可）。

### 新功能
- 蓝牙音频支持：提示音在 BT 音箱/耳机上完整播放，不再丢失前几百毫秒
- 音箱保活 & 麦克风保活开关，防止 BT 设备休眠断开
- 麦克风 & 提示音输出设备选择，可指定录音和播放设备
- 鼠标中键/侧键可用作录音快捷键
- 文本注入兼容性改进：支持 Electron (VS Code)、微信、飞书等更多应用
- 剪贴板行为改进：录音不再意外覆盖原有剪贴板内容
- 新增提示音风格：拨弦、沉浸、乒
- 双语 README (中文 + English)

### 改进
- ASR 停止流程优化：录完到出结果延迟显著降低
- 超短音频 (<0.3s) 自动跳过，减少噪声幻觉
- 浮窗进度条改为两阶段动画，体验更流畅
- 移除订阅/Cloud 代理功能，开源版更轻量

## v1.8.1 — ElevenLabs 修复 + 百炼国际版支持 (2026-04-07)

### Bug 修复
- 修复 ElevenLabs 录音结束后最长延迟 5 秒才注入文字的问题 (#105)
- 修复 ElevenLabs 流式识别累积模式下文字重复的问题 (#105)

### 新功能
- 百炼 ASR 新增 Base URL 配置项，支持国际版端点 (#106)

## v1.8.0 — 语音润色升级 + Qwen3 校准 + 断句优化 (2026-04-06)

### 语音润色
- Prompt 全面升级：支持自我修正识别（"不对，应该是..."自动处理）、口语数字转阿拉伯数字、多要点自动分点结构化、语境感知（正式/非正式内容区别处理）
- 语音润色升级为内置模式，模式详情页支持「还原为官方版」一键恢复
- 新增短文本跳过润色选项（10-50 字阈值可选），短句直接用 ASR 结果

### 语音识别
- SenseVoice 录音结束后自动调用 Qwen3 HTTP API 做二次校准，提升最终识别准确率
- 火山引擎断句参数优化：end_window_size 1.5s→3s，force_to_speech_time 关闭，减少思考停顿被截断
- Soniox 端点检测延迟 3s→10s，适应更长停顿
- ASR 标点始终开启，不再因有 LLM ���关闭
- CJK 字符间多余空格自动清除（ASR 分段拼接产生的空格）

### 交互改进
- 新增去句末标点选项（关闭/仅句号/全部标点）
- ESC 打断改进：`onESCAbort` 返回处理状态，无活跃录音时 ESC 穿透到系统
- ESC 打断始终启用，移除用户设置开关

### 音频反馈
- 开始音改用 cachedPlayer，新增静音预热（primeAudioOutput），首次提示音不再丢失开头
- 停止音按样式独立处理（chime/水滴1/水滴2/键盘/关闭）

### 设置 UI
- 本地模型区域重设计为双栏卡片：SenseVoice（始终运行）+ Qwen3-ASR（可控启停）
- 模式编辑表单样式统一，Prompt 编辑器自动高度
- 历史记录新增「纠错」按钮，快速打开纠错弹窗
- 切��� ASR 引擎时自动释放 SenseVoice 缓存模型

## v1.7.0 — 社区 PR 合并 + Keychain 迁移 + 注入性能优化 (2026-04-04)

### 社区贡献
- 历史记录显示 ASR 引擎名称 (#99, @jovezhong)
- Deepgram 数字转换开关 (#100, @jovezhong)
- ElevenLabs Scribe v2 流式识别引擎 (#101, @jovezhong)
- API Key 迁移到 macOS Keychain + 日志脱敏 (#102, @jasonwong2001)
- 空录音不保存历史 + 按钮点击区域优化 (#103, @ShaneLevs)

### 词汇管理
- 内置热词/片段脱钩，改为纯用户管理
- 热词和片段替换支持批量编辑（Sheet 弹窗，每行一条）
- 批量编辑按钮移到标题行，与排序按钮并排
- Soniox 二次校准死代码清理

### 性能优化
- 文本注入后立即通知 UI，剪贴板恢复延迟执行，减少粘贴→完成的感知延迟
- 快捷键停止和 ESC 中断改用 MainActor.assumeIsolated，减少调度延迟
- 防止旧 session 的 stale finalized 覆盖新录音

### 构建
- DMG 构建自动检测 variant/sherpa 状态变化，清理过期缓存
- 签名跳过优化：同 identity + 有效签名时不重签

## v1.6.2 — Soniox 重构 + 异步校准 + 并发安全 (2026-04-01)

- Soniox 客户端重构：去掉 ConnectionGate/Delegate，简化为直连 WebSocket
- Soniox 异步校准：录音结束后并行启动完整音频转录，自动替换更准确的结果
- Soniox 协议优化：start message 加 language_hints (zh/en)、max_endpoint_delay_ms
- Soniox 配置简化：移除 model 选择字段，默认使用最新模型
- SonioxAsyncClient：新增文件级异步转录（上传 → 轮询 → 获取结果）
- 火山引擎热词逻辑优化：有云端词表 (boosting_table_id) 时跳过 inline hotwords，避免冲突
- 并发安全修复：KeychainService 凭证缓存、SnippetStorage/HotwordStorage 文件缓存加锁
- SystemVolumeManager：CoreAudio 操作移到专用后台队列，避免蓝牙设备卡主线程
- SenseVoiceServerManager：currentQwen3Port 改用 OSAllocatedUnfairLock
- PromptContext.capture() 改为 async，AX 读取用 detached task + timeout 防死锁
- DebugFileLogger：ISO8601DateFormatter 复用，避免重复创建
- HotwordStorage：新增 builtinVersion 版本号 + loadCloudCompatible() 过滤方法

## v1.6.1 — 流式识别韧性 + 代理绕过 + 词库优化 (2026-03-31)

- 流式识别韧性大幅增强：按停止立即响应、不再重复粘贴、超时自动恢复
- 连接中途断开时自动用完整录音重新识别（batch fallback）
- 中断/失败的识别也保存到历史记录
- 新增「绕过系统代理」选项（关闭/仅 ASR/全部）
- Deepgram 热词受 URL 长度限制，自动截取前 30 个并在设置页提示
- 词库管理界面优化：替换映射按组显示、热词和替换映射支持排序
- ASR 设置：新 provider 自动填充默认值、凭证校验优化
- 自动更新修复：不再对已签名 DMG 重复签名（修复 Gatekeeper「已损坏」错误）
- AssemblyAI 多语言模型支持
- 6 个 ASR 客户端发送计数修正，避免误判连接状态

## v1.6.0 — 应用内更新 + Apple Speech + Bug 修复 (2026-03-30)

- 应用内更新：设置页 About 标签直接下载新版本并自动安装重启，Local 版更新时自动保留本地 ASR 模型
- 新增 Apple Speech 识别引擎：macOS 原生语音识别，无需 API Key，支持多语言
- 修复长录音（40-70s+）按快捷键停止时文字丢失：toggle 状态反转导致 onStart 触发 forceReset，现在安全重定向到 stop
- 火山引擎模型选项简化命名（"流式语音识别模型 2.0" → "模型 2.0（推荐，更便宜）"）
- ASR 服务启动时显示「启动中」状态提示
- 片段替换引擎优化，移除冗余映射词条和编译缓存
- CLAUDE.md 更新为 SenseVoice + Qwen3-ASR 双引擎架构描述

## v1.5.1 — Bug 修复 + 稳定性改进 (2026-03-30)

- 修复片段替换链式叠加 bug：正则缺少 word boundary，导致前一条替换的产物被后续规则二次匹配（如 "Cloud Code" → "Claudee Code"）
- 快捷键 event tap 健康检查：每 10 秒检测 tap 是否存活，静默失效时自动重建
- 辅助功能权限重试改进：5 次重试失败后弹窗提示重启 App，附带一键重启
- 新增 `type4me://reload-vocabulary` URL scheme，支持外部工具（如 Claude Code skill）触发热词/片段词表刷新
- 签名优化：首次构建自动创建持久化自签名证书 "Type4Me Local"，避免 ad-hoc 签名每次重编译后辅助功能权限失效
- 构建时自动移除 quarantine flag，防止 Accessibility 权限静默失效
- 设置向导增加辅助功能权限提示文案

## v1.5.0 — Dual-ASR + 三版本发布 (2026-03-30)

### 🎯 Dual-ASR 架构

全新双模型并行识别架构，大幅提升转写准确率：

- **SenseVoice** 负责流式实时识别，说话时即时出字
- **Qwen3-ASR** 负责精准校验，停顿时增量投机转录，松手后全量 final 校正
- 设置页新增两个模型独立启停按钮 + 状态显示
- 支持 Qwen3-only 模式（悬浮窗显示"录音中"）

### 📦 两种 DMG 版本

| 版本 | 包含内容 |
|------|---------|
| Cloud | 纯云端识别，最小体积 (~23MB) |
| Local | SenseVoice + Qwen3-ASR 双模型本地识别，开箱即用 (~1.2GB) |

### 🗂 存储架构升级

- 热词从 UserDefaults 迁移到双 JSON 文件 (builtin-hotwords.json + hotwords.json)
- 片段替换同步迁移 (builtin-snippets.json + snippets.json)
- 139 个内置默认热词
- 词汇表 UI 全新设计：内置词数统计、Finder 一键打开编辑、刷新按钮

### 🔧 LLM 管理

- 设置页新增 LLM 启停按钮
- `/llm/unload` 端点释放内存，`/llm/load` 重新加载
- LLM 与 ASR 共享 GPU 推理锁，避免并发 Metal 冲突

### 🐛 Bug 修复

- 进程泄漏：PID 文件管理替代 pgrep 误杀
- App 退出：同步 killAllServerProcesses 替代 fire-and-forget Task
- PyTorch detach()：3 处 `.numpy()` 前补 `.detach()`
- PyInstaller email-validator 依赖补全
- 配置持久化：`UserDefaults.bool` → `object(forKey:) as? Bool ?? true`
- onChange 初始化误触发防护
- SenseVoice 端口检测：`isRunning` → `currentPort != nil`

---

## v1.3.7 — 保留剪贴板 + Dock 图标 (2026-03-29)

### ✨ 新功能

- 新增「保留剪贴板」设置（偏好设置 → 通用 → 第二行）(#57)
  - 开启：使用键盘模拟输入，完全不碰剪贴板
  - 关闭（默认）：注入成功后自动恢复原始剪贴板，失败时保留识别文本作为 fallback
- 启动时显示 Dock 图标，关闭所有窗口后自动隐藏到菜单栏

### 🔧 改进

- 注入成功检测：非编辑角色从 4 个扩展到 27 个，减少误判
- 区分"无聚焦元素"（桌面）和"Electron nil role"（编辑区），避免桌面语音输入时文本丢失
- 剪贴板深拷贝支持所有类型（图片、文件、富文本），不只是纯文本

### 🐛 Bug 修复

- 修复剪贴板恢复时 changeCount 校验使用写入前的值导致恢复静默失败

---

## v1.3.5 — 修复菜单栏图标不显示 (2026-03-28)

### 🐛 Bug 修复

- 修复 macOS 26 Tahoe 安装后菜单栏图标不显示的问题（#54）
  - 根因：「Allow in Menu Bar」提示框每个版本只弹一次，卸载重装后 UserDefaults 仍保留导致提示不再出现
  - 改为每次启动都检测，只要图标不可见就提示用户去系统设置开启
- 修复多显示器环境下菜单栏图标可见性检测误判的问题
  - 原来只对主屏幕坐标范围做检测，多屏下可能漏报
  - 改为遍历所有已连接屏幕进行坐标判断

---

## v1.2.0 — Prompt 上下文变量 (2026-03-24)

### 🎯 Prompt 模板变量扩展

Prompt 模板新增 `{selected}` 和 `{clipboard}` 两个变量，让语音输入不再只是"听写"，而是可以**用语音对选中的文字下达命令**。

- **`{text}`**：语音识别的文字（原有）
- **`{selected}`**：录音开始时光标选中的文字（新增）
- **`{clipboard}`**：录音开始时剪切板的内容（新增）

#### 使用场景

语音识别的内容变成 LLM 的**命令**，选中的文字和剪切板变成**上下文**：

- 选中一段英文，按住快捷键说"翻译选中的文字"→ LLM 收到选中内容 + 翻译指令
- 复制一段代码到剪切板，说"解释一下剪切板里的代码"→ LLM 收到代码 + 解释指令
- 选中一段文字，说"把这段话改成正式的书面语"→ LLM 直接改写选中内容

### 📋 新增"命令模式"

内置新增"命令模式"处理模式，将语音输入作为 LLM 命令，结合 `{selected}` 和 `{clipboard}` 上下文执行：

```
命令如下：{text}
选择的内容：{selected}
剪切板的内容：{clipboard}
```

### 🛡 Accessibility 调用超时保护

读取选中文字通过 macOS Accessibility API 实现，部分应用（如 Electron 应用）的 AX 响应可能卡顿。已增加 200ms 超时保护，避免系统 UI 冻结。

---

## v1.1.0 — 本地语音识别 (2026-03-24)

### 🎯 本地 ASR（Paraformer / Zipformer）

无需申请云端 API Key，开箱即用。基于 SherpaOnnx 引擎，所有推理完全在设备端完成，无网络依赖。在 Apple Silicon (M1/M2/M3/M4) 大内存机型上表现尤佳。

- **三种模型可选**：
  - 极速轻量（~20 MB）：最小模型，适合快速输入，精度一般
  - 均衡推荐（~236 MB）：14000 小时训练数据，精度与体积的最佳平衡
  - 中英双语（~1 GB）：精度最高，支持中英文混合识别
- **流式 + 离线双通道**：流式识别实时出字，录音结束后离线模型二次修正
- **自动标点**：CT-Transformer 标点模型自动补全标点符号
- **模型管理 UI**：下载进度条、模型切换、测试、删除，一站管理
- **断点续传**：大文件下载中断后自动恢复，最多 20 次重试
- **首字优化**：录音开头跳过 400ms 静音/提示音区间，避免首字丢失或误识别

### 🔔 提示音自定义

录音开始提示音从开关升级为多选项：
- 电子提示音（原有）
- Water Drop 1（新增）
- Water Drop 2（新增）
- 关闭

### 🛠 其他改进

- 模型删除增加二次确认，防止误操作
- 未下载模型不再显示选择圆圈，下载按钮左置，UI 更直观
- 测试按钮移至识别引擎标题行右侧，远离删除区域
- ASR Provider 枚举新增 `.sherpa`，本地识别作为一等公民集成进 Provider 体系
